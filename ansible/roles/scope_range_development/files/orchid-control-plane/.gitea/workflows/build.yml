name: Build ORCHID API

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Clone the synthetic source
        run: |
          rm -rf /tmp/orchid-control-plane
          git clone http://10.50.10.40:3000/rangeadmin/orchid-control-plane.git /tmp/orchid-control-plane

      - name: Test the service
        run: |
          cd /tmp/orchid-control-plane
          PYTHONPATH=. python3 -m unittest discover -s tests -v

      - name: Build and publish the image
        run: |
          cd /tmp/orchid-control-plane
          docker build -t localhost:5000/orchid-api:0.1.0 .
          docker tag localhost:5000/orchid-api:0.1.0 localhost:5000/orchid-api:latest
          docker push localhost:5000/orchid-api:0.1.0
          docker push localhost:5000/orchid-api:latest
